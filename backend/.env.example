# Claude Code 工作目录
WORKSPACE_DIR=~/workspace

# 上下文压缩配置
CONTEXT_WINDOW=200000        # 模型上下文窗口大小
COMPACTION_RATIO=0.4         # 压缩触发比例（窗口 x 比例 = 阈值）
COMPACTION_MAX_TURNS=30      # 轮次兜底
COMPACTION_IDLE_MINUTES=30   # 空闲触发（分钟）
